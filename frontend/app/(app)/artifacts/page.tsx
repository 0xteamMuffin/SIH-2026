export default function ArtifactsPage() {
  return (
    <div className="stub-page">
      <h1>Artifacts</h1>
      <p className="text-muted">List and download generated files — needs GET /api/workspaces/:id/artifacts backend route.</p>
    </div>
  );
}
